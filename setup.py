from setuptools import setup

setup(name='scripts',
      version='1.0',
      description='Scripts for the project',
      url='',
      author='me',
      license='MIT',
      packages=['scripts'],
      zip_safe=False)
